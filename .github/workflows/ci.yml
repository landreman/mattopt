name: CI

on: [push, pull_request]

jobs:
  CI:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: pwd
      run: pwd
    - name: ls
      run: ls
    - name: python version
      run: python --version
    - name: unit tests
      run: python -m unittest discover
